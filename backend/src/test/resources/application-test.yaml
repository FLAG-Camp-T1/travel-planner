spring:
  datasource:
    url: jdbc:h2:mem:travelplanner;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

app:
  bootstrap:
    seed-enabled: false
